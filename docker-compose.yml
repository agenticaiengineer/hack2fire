services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hack2fire
      POSTGRES_USER: hack2fire
      POSTGRES_PASSWORD: hack2fire
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hack2fire -d hack2fire"]
      interval: 5s
      timeout: 5s
      retries: 20

  web:
    build:
      context: .
      target: local
    environment:
      DATABASE_URL: postgresql://hack2fire:hack2fire@db:5432/hack2fire?schema=public
      SESSION_SECRET: local-docker-session-secret-change-before-production
      NEXT_PUBLIC_APP_URL: http://localhost:3000
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres-data:
